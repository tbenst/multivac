{
  "scripts": {
    "postinstall": "spago install",
    "clean": "rm -rf node_modules output .spago dist/* *.lock",
    "build": "spago build",
    "watch": "spago build --watch",
    "dev": "yarn parcel assets/*.html",
    "bundle": "spago bundle-app --main Multivac.Main --to dist/app.js && spago bundle-app --main Multivac.Main --to dist/app.js && parcel  assets/*.html",
    "serve": "yarn bundle && http-server dist",
    "test": "spago test"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "purescript": "^0.13.5",
    "spago": "^0.12.1"
  },
  "dependencies": {
    "http-server": "^0.12.0"
  }
}
